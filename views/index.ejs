<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 flex flex-col items-center py-10">
        <div class="form p-6 bg-zinc-800 rounded-lg text-white w-full max-w-xl">
            <form action="/create" method="post">
                <input class="block w-full rounded-md outline-none px-5 py-2 bg-zinc-700" type="text" placeholder="Title" name="title" required>
                <textarea class="block w-full rounded-md outline-none px-5 py-2 mt-2 bg-zinc-700 resize-none" placeholder="Write here..." name="details" ></textarea>
                <input class="mt-4 bg-blue-600 hover:bg-blue-500 px-5 py-2 rounded-md cursor-pointer" type="submit" value="Save">
            </form>
        </div>
        
        <div class="tasks flex flex-wrap justify-evenly gap-5 p-10 w-full max-w-6xl">
            <% if (files.length > 0) { %>
                <% files.forEach((file) => { %>
                    <div class="task w-full sm:w-72 bg-zinc-800 rounded-md hover:bg-zinc-700 transition duration-200 relative">
                        <a href="/files/<%= file.fileName %>" class="block m-3">
                            <h1 class="text-white text-2xl font-semibold tracking-tight overflow-hidden text-ellipsis whitespace-nowrap">
                                <%= file.fileName.replace('-7-98-', ' ').substring(19).slice(0, -4) %>
                            </h1>
                            <p class="text-zinc-400 text-sm tracking-tighter overflow-hidden text-ellipsis whitespace-nowrap">
                                <% if (file.content.length === 0) { %>
                                    <span class="text-zinc-600 text-sm mt-1">No Content</span>
                                <% } else { %>
                                    <%= file.content %>
                                <% } %>
                            </p>
                        </a>
                        <div class="flex justify-between m-3">
                            <div class="flex space-x-2">
                                <a href="/files/<%= file.fileName %>" class="text-blue-500">Read More</a>
                                <a href="/edit/<%= file.fileName %>" class="text-zinc-400 hover:text-blue-500">Edit</a>
                            </div>
                            <form action="/delete/<%= file.fileName %>" method="delete" class="inline">
                                <button type="submit" class="text-white hover:text-red-600">Delete</button>
                            </form>
                        </div>
                    </div> 
                <% }) %>            
            <% } else { %>
                <h3 class="text-zinc-600">No tasks</h3>
            <% } %>
        </div>
    </div>
</body>
</html>
